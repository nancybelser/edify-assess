---
 apiVersion: v1
 kind: Pod
 metadata:
   name: devops-assessment-nancy
   labels:
     app: web
 spec:
   containers:
     - name: node-nancy
       image: nancybelser/node-web-app
       ports:
         - containerPort: 8080